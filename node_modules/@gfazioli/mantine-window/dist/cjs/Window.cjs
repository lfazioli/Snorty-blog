'use client';
'use strict';

var React = require('react');
var iconsReact = require('@tabler/icons-react');
var core = require('@mantine/core');
var useMantineWindow = require('./hooks/use-mantine-window.cjs');
var Window_module = require('./Window.module.css.cjs');

const defaultProps = {
  withBorder: true,
  shadow: "md",
  resizable: "both",
  draggable: "both",
  collapsed: false,
  withCloseButton: true,
  withCollapseButton: true,
  collapsable: true,
  persistState: false,
  withinPortal: true,
  minWidth: 250,
  minHeight: 100
};
const varsResolver = core.createVarsResolver((_, { radius, shadow }) => {
  return {
    root: {
      "--window-background": "var(--mantine-color-default)",
      "--window-radius": radius === void 0 ? "var(--mantine-radius-lg)" : core.getRadius(radius),
      "--window-shadow": core.getShadow(shadow)
    },
    container: {},
    content: {},
    header: {},
    title: {},
    closeButton: {},
    collapseButton: {},
    windowToolsButton: {},
    resizeHandleTopLeft: {},
    resizeHandleTop: {},
    resizeHandleTopRight: {},
    resizeHandleRight: {},
    resizeHandleBottomRight: {},
    resizeHandleBottom: {},
    resizeHandleBottomLeft: {},
    resizeHandleLeft: {}
  };
});
const Window = core.factory((_props, _) => {
  const props = core.useProps("Window", defaultProps, _props);
  const {
    opened,
    color,
    title,
    children,
    resizable,
    draggable,
    collapsed,
    withCollapseButton,
    collapsable,
    withCloseButton,
    onClose,
    radius,
    withinPortal,
    persistState,
    minWidth,
    minHeight,
    maxWidth,
    maxHeight,
    dragBounds,
    defaultPosition,
    defaultSize,
    onPositionChange,
    onSizeChange,
    withBorder,
    mod,
    classNames,
    style,
    styles,
    unstyled,
    vars,
    className,
    ...others
  } = props;
  const getStyles = core.useStyles({
    name: "Window",
    props,
    classes: Window_module,
    className,
    style,
    classNames,
    styles,
    unstyled,
    vars,
    varsResolver
  });
  const {
    bringToFront,
    windowRef,
    position,
    size,
    isCollapsed,
    setIsCollapsed,
    isVisible,
    zIndex,
    handleMouseDownDrag,
    handleTouchStartDrag,
    resizeHandlers,
    handleClose
  } = useMantineWindow.useMantineWindow(props);
  const draggableHeader = draggable === "header" || draggable === "both";
  const draggableWindow = draggable === "window" || draggable === "both";
  if (!isVisible) {
    return null;
  }
  return /* @__PURE__ */ React.createElement(
    core.Box,
    {
      ref: windowRef,
      onClick: bringToFront,
      mod: [{ "data-with-border": withBorder, "data-window-draggable": draggableWindow }, mod],
      onMouseDown: draggableWindow ? handleMouseDownDrag : void 0,
      onTouchStart: draggableWindow ? handleTouchStartDrag : void 0,
      bg: color,
      ...others,
      ...getStyles("root", {
        style: {
          position: withinPortal ? "fixed" : "absolute",
          left: position.x,
          top: position.y,
          width: size.width,
          height: isCollapsed ? "auto" : size.height,
          zIndex
        }
      })
    },
    /* @__PURE__ */ React.createElement(core.Box, { ...getStyles("container") }, /* @__PURE__ */ React.createElement(
      core.Box,
      {
        ...getStyles("header"),
        onClick: bringToFront,
        mod: { "window-draggable": draggableHeader },
        onMouseDown: draggableHeader ? handleMouseDownDrag : void 0,
        onTouchStart: draggableHeader ? handleTouchStartDrag : void 0,
        onDoubleClick: () => collapsable && setIsCollapsed(!isCollapsed)
      },
      /* @__PURE__ */ React.createElement(core.Flex, { align: "center", gap: "xs", miw: 0 }, /* @__PURE__ */ React.createElement(core.Flex, { align: "center", gap: 8 }, withCloseButton && /* @__PURE__ */ React.createElement(
        core.ActionIcon,
        {
          radius: 256,
          color: "red",
          onClick: handleClose,
          ...getStyles("closeButton")
        },
        /* @__PURE__ */ React.createElement(iconsReact.IconX, { size: 14 })
      ), withCollapseButton && collapsable && /* @__PURE__ */ React.createElement(
        core.ActionIcon,
        {
          radius: 256,
          color: "yellow",
          onClick: () => setIsCollapsed(!isCollapsed),
          ...getStyles("collapseButton")
        },
        isCollapsed ? /* @__PURE__ */ React.createElement(iconsReact.IconPlus, { size: 14 }) : /* @__PURE__ */ React.createElement(iconsReact.IconMinus, { size: 14 })
      )), /* @__PURE__ */ React.createElement(core.Flex, { align: "center", gap: "xs", miw: 0 }, /* @__PURE__ */ React.createElement(core.Text, { truncate: true, ...getStyles("title") }, title)))
    ), !isCollapsed && /* @__PURE__ */ React.createElement(React.Fragment, null, /* @__PURE__ */ React.createElement(
      core.ScrollArea,
      {
        ...getStyles("content", {
          style: {
            height: size.height - 40
          }
        })
      },
      children
    ), resizable !== "none" && /* @__PURE__ */ React.createElement(React.Fragment, null, resizable === "both" && /* @__PURE__ */ React.createElement(React.Fragment, null, /* @__PURE__ */ React.createElement(
      core.Box,
      {
        "data-resize-handle": true,
        ...resizeHandlers.topLeft,
        ...getStyles("resizeHandleTopLeft")
      }
    ), /* @__PURE__ */ React.createElement(
      core.Box,
      {
        "data-resize-handle": true,
        ...resizeHandlers.topRight,
        ...getStyles("resizeHandleTopRight")
      }
    ), /* @__PURE__ */ React.createElement(
      core.Box,
      {
        "data-resize-handle": true,
        ...resizeHandlers.bottomRight,
        ...getStyles("resizeHandleBottomRight")
      }
    ), /* @__PURE__ */ React.createElement(
      core.Box,
      {
        "data-resize-handle": true,
        ...resizeHandlers.bottomLeft,
        ...getStyles("resizeHandleBottomLeft")
      }
    )), (resizable === "vertical" || resizable === "both") && /* @__PURE__ */ React.createElement(React.Fragment, null, /* @__PURE__ */ React.createElement(
      core.Box,
      {
        "data-resize-handle": true,
        ...resizeHandlers.top,
        ...getStyles("resizeHandleTop")
      }
    ), /* @__PURE__ */ React.createElement(
      core.Box,
      {
        "data-resize-handle": true,
        ...resizeHandlers.bottom,
        ...getStyles("resizeHandleBottom")
      }
    )), (resizable === "horizontal" || resizable === "both") && /* @__PURE__ */ React.createElement(React.Fragment, null, /* @__PURE__ */ React.createElement(
      core.Box,
      {
        "data-resize-handle": true,
        ...resizeHandlers.right,
        ...getStyles("resizeHandleRight")
      }
    ), /* @__PURE__ */ React.createElement(
      core.Box,
      {
        "data-resize-handle": true,
        ...resizeHandlers.left,
        ...getStyles("resizeHandleLeft")
      }
    )))))
  );
});
Window.classes = Window_module;
Window.displayName = "Window";

exports.Window = Window;
exports.defaultProps = defaultProps;
//# sourceMappingURL=Window.cjs.map
